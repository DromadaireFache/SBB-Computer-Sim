func[3] main(var arg1, var[3] arg2) {
    var x;
    return 0;
}

func foo() {
    //this is just a random function

    var x;
    var[3] y;
    x = y;
    main(x,y);
    if (x>y) {
        while (x < 10) {
            x = x + 1;
        }
        return x - y;
    }
}